name: qairlane
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^2.4.0
  shared_preferences: ^2.2.0
  flutter_bloc: ^8.1.3 # Для управления состоянием
  equatable: ^2.0.5
  get_it: ^7.6.0 # Для DI
  flutter_blurhash: ^0.7.0
  animated_text_kit: ^4.2.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
